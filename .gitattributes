# Keep text files as LF in the repo and working tree so the shell hooks and CI
# run correctly on the Linux GitHub Actions runners regardless of the OS.
* text=auto eol=lf

# Binary assets - never apply end-of-line conversion to these.
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.ico  binary
